# Shell scripts must stay LF even on Windows checkouts (core.autocrlf=true).
*.sh text eol=lf

# PowerShell and markdown can round-trip through the default normalization.
*.ps1 text eol=crlf
*.md text
